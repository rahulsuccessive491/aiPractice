# Copy to .env.local for local dev (leave blank — Vite proxy handles /api)
VITE_API_URL=

# For production builds point at your Render backend:
# VITE_API_URL=https://ai-skills-portal-api.onrender.com
